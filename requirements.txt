# Core
pandas==2.3.3
numpy==2.0.2
scikit-learn==1.6.1
xgboost==2.1.4
matplotlib==3.9.4

# Notebook (optional, only needed to run notebooks/)
jupyter
nbconvert

# Data rebuild (optional, only needed for src/build_descriptors.py; also requires a Java runtime)
padelpy
